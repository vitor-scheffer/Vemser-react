const Select = ({label, option1, option2}) => {
  return (
    <div className='input'>
      <label htmlFor="">{label}</label>
      <select name="" id="">
        <option value="">{option1}</option>
        <option value="">{option2}</option>
      </select>
    </div>
    
  )
}

export default Select